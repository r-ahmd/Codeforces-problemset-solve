#include <bits/stdc++.h>
using namespace std;

int main() 
{
  long long n, m, a;
  cin >> n >> m >> a;

  long long rows = n / a + (n % a != 0);
  long long cols = m / a + (m % a != 0);
  long long result = rows * cols;

  cout << result << endl;

  return 0;
}







